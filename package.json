{
  "name": "agent-first-front-door",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that classifies inbound traffic and serves agents a structured action payload while passing humans through to the origin.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "engines": {
    "node": ">=18"
  }
}
